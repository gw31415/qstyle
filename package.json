{
  "name": "@qstyle/root",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.2.1",
  "engines": {
    "node": ">=22.13",
    "pnpm": ">=12"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test && node scripts/size-report.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "changeset-status": "echo \"no changesets yet\""
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "fast-check": "^4.9.0",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
